# branching_model — backport strategy

A reference repository illustrating a **backport** release workflow, where
fixes land on `main` and are backported onto maintenance release branches.

![Backport branching model](backport.svg)
